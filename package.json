{
  "name": "skills-demo-csnw",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "client"
  ],
  "scripts": {
    "start-dev": "concurrently \"npm run start --workspace client\" \"npm run dev --workspace backend\"",
    "client-dev": "npm start --workspace client",
    "backend-dev": "npm dev --workspace backend",
    "start": "npm run build --workspace client && npm run start --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}